{
  "name": "menti_site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && powershell -NoProfile -ExecutionPolicy Bypass -Command \"Remove-Item -Recurse -Force docs -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Force docs | Out-Null; Copy-Item -Path dist\\* -Destination docs -Recurse -Force; Copy-Item dist\\app.html docs\\index.html -Force; Remove-Item docs\\app.html -ErrorAction SilentlyContinue; New-Item -ItemType File -Force docs\\.nojekyll | Out-Null\"",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.2.2",
    "prettier": "^3.3.1",
    "vite": "^5.2.0"
  }
}
